AES-128-CBC search applications including:

* Sequential
* OpenMP Parallelisation
* MPI Parallelisation

Each search method is inside its own directory with instructions on how to build inside the README.md